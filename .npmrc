# Allow optional dependencies (needed for oxc-parser native bindings)
# Use pnpm-style hoisting
shamefully-hoist=true
# Don't fail on engine mismatches
engine-strict=false
# Skip fund and audit messages
fund=false
audit=false
# Ignore scripts that might fail
ignore-scripts=false
