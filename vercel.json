{
  "buildCommand": "cd apps/web && pnpm build",
  "installCommand": "npm install -g pnpm@9.15.0 && pnpm install --no-optional --ignore-scripts && cd apps/web && pnpm exec nuxt prepare",
  "framework": "nuxtjs",
  "functions": {
    "apps/web/app/**": {
      "runtime": "nodejs20.x"
    }
  },
  "env": {
    "npm_config_optional": "false",
    "npm_config_omit": "optional",
    "PNPM_REGISTRY": "https://registry.npmjs.org/"
  }
}

